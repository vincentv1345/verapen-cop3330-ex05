package org.example;

public class PrintFunctions extends MathFunctions {
    public void printFunction(double number1, double number2) {
        add(number1, number2);
        subtract(number1, number2);
        multiply(number1, number2);
        divide(number1, number2);
    }
}
